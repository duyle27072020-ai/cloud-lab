services:
  backend:
    build:
      context: ./server
    container_name: mern-backend
    ports:
      - "5000:5000"
    env_file:
      - .env

  frontend:
    build:
      context: ./client
    container_name: mern-frontend
    ports:
      - "4173:4173"
    depends_on:
      - backend